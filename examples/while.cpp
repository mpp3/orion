#include "dyno.h"
using namespace std;

int main()
{
	// Escribe un programa que escriba 10 veces la palabra hola

	int i {0};
	while (i < 10) {
		cout << "hola\n";
		i++;
	}

	// Escribe un programa que cuente desde 1 hasta 10


	// Escribe un bucle que cuente desde 100 hasta 1


	// Escribe un bucle que escriba los números pares desde 1 hasta 100


	// Escribe un bucle que escriba los números entre 1 el que quiera el usuario


	// ¿Cuántas veces se ejecutará el siguiente bucle?
	// int m {10};
	// while (m < 10) {
	// 	cout << m << "\n";
	// 	m++;
	// }

	// Escribe un bucle que no se ejecute ninguna vez

	// Escribe un bucle que se ejecute infinitamente

	// ¿Cuántas veces se ejecutará este bucle?
	// int x {0};
	// x = 0; // Asignación: cambia el valor de x a 0
	// x == 0; // Comparación: compara x con 0
	// while (x = 0) {
	// 	cout << "Crees que verás este mensaje?";
	// }
	// cout << (x = 5) << "\n";
	// cout << (x == 6) << "\n";

	// ¿Cuántas veces se ejecutará el siguiente bucle?

	// while (1) {
	// 	cout << "Esto no hay quien lo pare.\n";
	// }

	// ¿Cuántas veces se ejecutará este bucle?

	// bool veleta {true};
	// while(veleta) {
	// 	veleta = !veleta;
	// }

	// ¿Y este?

	// char car {'a'};
	// while (letra(car)) {
	// 	cout << car << " ";
	// 	car++;
	// }
}